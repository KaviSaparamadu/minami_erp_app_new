// Your PC's LAN IP (from `ipconfig` → Wi-Fi IPv4 Address).
// Phone must be on the SAME Wi-Fi network as the PC running the API.
// If you switch Wi-Fi / restart your router, re-check `ipconfig` and update this.
// Important: Include /api in the path to match backend routes
export const API_BASE_URL = 'https://minami-erp.gpit.io';
